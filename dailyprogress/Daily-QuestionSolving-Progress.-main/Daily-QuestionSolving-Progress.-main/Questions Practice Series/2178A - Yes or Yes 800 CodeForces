#include <bits/stdc++.h>
using namespace std;
int main() {
    int t;
    cin >> t;
    while (t--) {
        string s;
        cin >> s;
        int count_y = 0;
        for (char c : s) {
            if (c == 'Y') {
                count_y++;
            }
        }
        if (count_y <= 1) {
            cout << "YES\n";
        } else {
            cout << "NO\n";
        }
    }
    return 0;
}
